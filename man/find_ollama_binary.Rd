% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{find_ollama_binary}
\alias{find_ollama_binary}
\title{Locate the Ollama binary on disk}
\usage{
find_ollama_binary()
}
\value{
Full path to the binary, or \code{""} if none found.
}
\description{
\code{Sys.which("ollama")} returns \code{""} when the parent process's PATH
doesn't include the install location. This bites macOS GUI R.app
users hard: brew installs Ollama at \verb{/usr/local/bin/ollama} (Intel)
or \verb{/opt/homebrew/bin/ollama} (Apple Silicon), and the double-click
R.app launcher inherits a truncated PATH that omits both. Users
with a fully working \code{ollama} in Terminal end up with a \code{screenllm}
app that refuses to start because it can't find the binary.
}
\details{
This helper first tries \code{Sys.which()}, then probes a list of
known install locations per platform.
}
\keyword{internal}
