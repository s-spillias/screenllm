% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpu.R
\name{gpu_status}
\alias{gpu_status}
\title{Live NVIDIA GPU status snapshot}
\usage{
gpu_status(throttled_clock_mhz = 800, loaded_mib = 500)
}
\arguments{
\item{throttled_clock_mhz}{Threshold below which the graphics
clock is considered throttled. Defaults to 800 MHz (a modern
dGPU under real inference load should be 1500-2500 MHz).}

\item{loaded_mib}{Threshold at which VRAM is considered "in use
by a model" (avoids false-positive throttling at rest, when
idle clocks are expected and normal). Defaults to 500 MiB.}
}
\value{
A list with \code{available}, \code{graphics_clock_mhz},
\code{memory_clock_mhz}, \code{memory_used_mib}, \code{memory_total_mib},
\code{power_draw_w}, \code{utilisation_pct}, \code{throttled} (logical), and
\code{hint} (short human-readable action if throttled).
}
\description{
Queries \code{nvidia-smi} for current graphics clock, memory clock,
memory used, power draw and utilisation. Used to catch the
"throttled" state where a laptop dGPU reports 99\% utilisation but
is running its cores at idle clock speeds (typically because the
laptop is on battery, in a power-saver profile, or persistence
mode is off). In that state throughput drops ~10x and each LLM
call takes 20-30s instead of 2-3s.
}
\details{
Returns \code{available = FALSE} on non-NVIDIA systems; the Metal /
ROCm equivalents don't expose live clock data through a portable
CLI.
}
\examples{
\dontrun{
s <- gpu_status()
if (isTRUE(s$throttled)) message(s$hint)
}
}
