% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{launch_screening_app}
\alias{launch_screening_app}
\title{Launch the interactive screening Shiny app}
\usage{
launch_screening_app(
  plan,
  ranked,
  out_file = "screening_decisions.csv",
  launch_browser = interactive()
)
}
\arguments{
\item{plan}{A \code{screenllm_plan} object.}

\item{ranked}{The full ranking (needed for the per-criterion
justifications).}

\item{out_file}{Path where decisions are written (\code{.csv} or \code{.xlsx}).}

\item{launch_browser}{Passed to \code{shiny::runApp()}.}
}
\value{
Invisibly, the path to \code{out_file}.
}
\description{
Opens a browser-based UI that walks the reviewer through the records
at or above the SAFE stop point. Each record is presented with title,
abstract, LLM ensemble score, and the per-criterion justifications.
Decisions are appended to \code{out_file} after every click so that a browser
crash never loses more than one decision.
}
