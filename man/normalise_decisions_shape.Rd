% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{normalise_decisions_shape}
\alias{normalise_decisions_shape}
\title{Coerce a decisions data.frame to the schema the rest of the
package expects: \code{id}, \code{human_decision}, \code{note}, \code{timestamp}.}
\usage{
normalise_decisions_shape(d)
}
\arguments{
\item{d}{A data.frame or NULL.}
}
\value{
A data.frame with at least the four canonical columns.
}
\description{
Any column missing from the input is added as a character NA
vector. Extra columns are preserved. \code{NULL} or a zero-row input
returns a properly-shaped empty tibble; the caller can \code{rbind} /
\code{bind_rows} new rows against it without a column-mismatch error.
}
\details{
Used at load time (mod_setup rehydration) and at write time
(Screen tab record_decision) so a legacy 1- or 2-column decisions
file on disk gets sanitised on its way through the app rather
than propagating broken shape into downstream tabs.
}
\keyword{internal}
