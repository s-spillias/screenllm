% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_openrouter.R
\name{backend_openrouter}
\alias{backend_openrouter}
\title{OpenRouter backend}
\usage{
backend_openrouter(
  api_key = Sys.getenv("OPENROUTER_API_KEY"),
  base_url = "https://openrouter.ai/api/v1",
  http_referer = NULL,
  x_title = NULL,
  timeout = 300,
  max_retries = 3L
)
}
\arguments{
\item{api_key}{OpenRouter API key. Defaults to
\code{Sys.getenv("OPENROUTER_API_KEY")}.}

\item{base_url}{Base URL for OpenRouter's API.}

\item{http_referer}{Optional attribution header. Set to the URL of
your project.}

\item{x_title}{Optional attribution header. Human-readable app name.}

\item{timeout}{Per-call timeout in seconds.}

\item{max_retries}{Retries on transport or 5xx errors.}
}
\value{
A backend object usable with \code{custom_ensemble()}.
}
\description{
OpenRouter exposes hundreds of open- and closed-source models
behind an OpenAI-compatible \verb{/chat/completions} API. Model tags
are namespaced by provider, e.g. \code{"openai/gpt-5"},
\code{"anthropic/claude-sonnet-5"}, \code{"meta-llama/llama-3.1-70b-instruct"},
\code{"deepseek/deepseek-r1"}, \code{"google/gemini-2.5-flash"}.
}
\details{
Two attribution headers (\code{HTTP-Referer}, \code{X-Title}) are optional but
recommended -- they get your traffic shown on your OpenRouter
account rankings without affecting behaviour.

The \code{api_key} is read from \code{OPENROUTER_API_KEY} by default.
}
\examples{
\dontrun{
# ~/.Renviron: OPENROUTER_API_KEY=sk-or-...
ens <- custom_ensemble(
  models = c("openai/gpt-5", "anthropic/claude-sonnet-5",
             "google/gemini-2.5-flash"),
  replicates = 1L,
  backend = backend_openrouter(x_title = "screenllm")
)
}
}
