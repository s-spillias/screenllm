% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async_pilot.R
\name{start_pilot_job}
\alias{start_pilot_job}
\title{Start a background pilot job}
\usage{
start_pilot_job(records, criteria, ensemble, n = 20L, sample = TRUE, seed = 1L)
}
\arguments{
\item{records}{A tibble of records (from \code{read_records()}).}

\item{criteria}{A \code{screenllm_criteria} object.}

\item{ensemble}{A \code{screenllm_ensemble} object.}

\item{n}{Number of records to score. Defaults to 20.}

\item{sample}{Whether to sample at random (default) or take the
first \code{n} rows.}

\item{seed}{Random seed for the sample.}
}
\value{
A list with \code{pid}, \code{handle}, and \code{progress_path}.
}
\description{
Spawns a \code{callr::r_bg} subprocess that samples \code{n} records from
\code{records}, scores each one against the criteria using every model
in the ensemble at 1 replicate, aggregates, and appends the result
to a progress file. The caller polls \code{pilot_job_status()} for
updates and \code{pilot_job_cancel(handle)} to abort.
}
\details{
Unlike \code{pilot()}, this returns immediately with a handle. The
progress file is at \code{pilot_progress_path()} and is overwritten by
each new job.
}
