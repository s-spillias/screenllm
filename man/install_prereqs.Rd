% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{install_prereqs}
\alias{install_prereqs}
\title{One-stop setup for a fresh machine}
\usage{
install_prereqs(
  models = .PINNED_DEFAULT_MODELS,
  interactive = base::interactive(),
  wait_seconds = 60L,
  ollama_url = getOption("screenllm.ollama_url")
)
}
\arguments{
\item{models}{Character vector of Ollama model tags to ensure are
installed. Defaults to the four-LLM paper ensemble. Pass
\code{default_ensemble_light()$models} (or the \code{.PINNED_LIGHT_MODELS}
equivalent) for the laptop-friendly preset, or \code{NULL} to skip.}

\item{interactive}{Whether to prompt the user before running install
commands or pulling large models. Defaults to \code{base::interactive()}.
When \code{FALSE}, the function reports missing components but never
installs or downloads anything.}

\item{wait_seconds}{Seconds to wait for the Ollama daemon to come up
after (re)starting it. Defaults to 60.}

\item{ollama_url}{Base URL of the Ollama server.}
}
\value{
Invisible logical: \code{TRUE} if all prerequisites are ready
after the call, \code{FALSE} otherwise.
}
\description{
Walks a non-technical user through everything needed to run \code{screenllm}:
}
\details{
\enumerate{
\item Detects the OS and checks whether Ollama is installed.
\item If Ollama is missing, offers to install it (\code{brew} on macOS,
\code{winget} on Windows, the official install script on Linux). If the
user declines or the package manager is unavailable, opens
\url{https://ollama.com/download} in the browser.
\item Waits for the Ollama daemon to come up (up to \code{wait_seconds}).
\item Pulls each model in \code{models} that is not already installed.
\item Verifies with \code{check_setup()}.
}

Safe to re-run: it is a no-op if everything is already in place.
}
\examples{
\dontrun{
# Paper ensemble (~65 GB; needs a workstation):
install_prereqs()

# Laptop-friendly preset (~10 GB):
install_prereqs(models = c("gemma3:4b", "llama3.2:3b",
                           "qwen3:4b", "mistral:7b"))

# Skip the model pull (just check Ollama itself):
install_prereqs(models = NULL)
}
}
