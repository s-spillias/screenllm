% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_openai.R
\name{backend_openai}
\alias{backend_openai}
\title{OpenAI Chat Completions backend}
\usage{
backend_openai(
  api_key = Sys.getenv("OPENAI_API_KEY"),
  base_url = "https://api.openai.com/v1",
  timeout = 300,
  max_retries = 3L
)
}
\arguments{
\item{api_key}{OpenAI API key. Defaults to \code{Sys.getenv("OPENAI_API_KEY")}.}

\item{base_url}{Base URL for the OpenAI API. Defaults to the
official endpoint; override for Azure-OpenAI proxies or a
self-hosted OpenAI-compatible gateway.}

\item{timeout}{Per-call timeout in seconds.}

\item{max_retries}{Retries on transport or 5xx errors.}
}
\value{
A backend object usable with \code{custom_ensemble()}.
}
\description{
Talks to OpenAI's \verb{/v1/chat/completions} endpoint. Uses the
response_format = "json_object" JSON mode so the model returns
valid JSON matching the screening prompt's schema.
}
\details{
The \code{api_key} is read from \code{OPENAI_API_KEY} by default. Store the
key in your user-level \verb{~/.Renviron} so it isn't committed to code
or exposed in shell history:

\preformatted{
OPENAI_API_KEY=sk-...
}

Model tags follow OpenAI's naming (e.g. \code{"gpt-5"}, \code{"gpt-4o"},
\code{"gpt-4o-mini"}). Pass whatever tag your account has access to.
}
\examples{
\dontrun{
# Set your key first in ~/.Renviron: OPENAI_API_KEY=sk-...
ens <- custom_ensemble(
  models = c("gpt-5", "gpt-4o"),
  replicates = 1L,
  backend = backend_openai()
)
}
}
