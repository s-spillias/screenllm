% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_gemini.R
\name{backend_gemini}
\alias{backend_gemini}
\title{Google Gemini generateContent backend}
\usage{
backend_gemini(
  api_key = {
     k <- Sys.getenv("GOOGLE_API_KEY")
     if (!nzchar(k)) 
         k <-
    Sys.getenv("GEMINI_API_KEY")
     k
 },
  base_url = "https://generativelanguage.googleapis.com/v1beta",
  timeout = 300,
  max_retries = 3L
)
}
\arguments{
\item{api_key}{Google API key. Defaults to
\code{Sys.getenv("GOOGLE_API_KEY")}, then \code{GEMINI_API_KEY}.}

\item{base_url}{Base URL for the Gemini API.}

\item{timeout}{Per-call timeout in seconds.}

\item{max_retries}{Retries on transport or 5xx errors.}
}
\value{
A backend object usable with \code{custom_ensemble()}.
}
\description{
Talks to Google's \code{generativelanguage.googleapis.com/v1beta}
endpoint. Uses \code{responseMimeType = "application/json"} in the
generationConfig so Gemini returns clean JSON.
}
\details{
The \code{api_key} is read from \code{GOOGLE_API_KEY} by default (also
accepts \code{GEMINI_API_KEY} if that env var is set instead).

Model tags follow Gemini's IDs (e.g. \code{"gemini-2.5-pro"},
\code{"gemini-2.5-flash"}). Pass whatever tag your account has access
to.
}
\examples{
\dontrun{
# ~/.Renviron: GOOGLE_API_KEY=AIzaSy...
ens <- custom_ensemble(
  models = "gemini-2.5-flash",
  replicates = 1L,
  backend = backend_gemini()
)
}
}
