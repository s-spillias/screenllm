% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{summarise_screening}
\alias{summarise_screening}
\title{Summarise a completed screening session}
\usage{
summarise_screening(ranked, decisions, plan = NULL)
}
\arguments{
\item{ranked}{A ranking (output of \code{rank_records()}).}

\item{decisions}{A tibble with columns \code{id} and \code{human_decision}
(values \code{"Accept"} or \code{"Reject"}).}

\item{plan}{Optional \code{screenllm_plan} used to identify the stop point;
when supplied, \code{summarise_screening()} also reports the workload
fraction the plan intended.}
}
\value{
A \code{screenllm_report} object.
}
\description{
Combines the LLM ranking with a set of human decisions and reports the
realised recall (against the decisions the human made above the stop
point), the workload actually incurred, and diagnostic per-record
counts.
}
