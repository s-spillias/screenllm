% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{clear_cache}
\alias{clear_cache}
\title{Clear cached LLM scores for a project}
\usage{
clear_cache(project, model = NULL, delete_ranked = TRUE)
}
\arguments{
\item{project}{Project name.}

\item{model}{Optional Ollama tag. When \code{NULL} (default), clears
every cached score in the project. When supplied, only cached
scores whose stored \verb{$model} field matches are removed. Reading
each cache file is O(n_files) but each file is tiny (~1 KB).}

\item{delete_ranked}{If \code{TRUE} (default), also removes the
project's \code{ranked.rds} artefact so a re-run starts from a
clean slate.}
}
\value{
Invisibly, the number of cache files removed.
}
\description{
Removes score-cache files under a project's cache directory. Use
this when a model returned garbage during a ranking run and you
want to re-score with fresh calls; the cache would otherwise be
hit on the next \code{rank_records()} invocation.
}
\details{
By default clears every cached score (the most conservative
"start over" behaviour). Pass \code{model = "..."} to clear only that
model's cached scores across all records / replicates. Optionally
also deletes the persisted ranked artefact (\code{delete_ranked = TRUE})
so a stale ranking doesn't linger.
}
\examples{
\dontrun{
# Clear one model's cached scores after it misbehaved
clear_cache("my-review", model = "mistral:7b")

# Nuclear option: clear everything for a project
clear_cache("my-review")
}
}
