% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{custom_ensemble}
\alias{custom_ensemble}
\title{Define a custom LLM ensemble}
\usage{
custom_ensemble(
  models,
  replicates = .DEFAULT_REPLICATES,
  aggregator = c("mean", "median", "max", "topk_mean"),
  temperature = .DEFAULT_TEMPERATURE,
  backend = backend_ollama()
)
}
\arguments{
\item{models}{Character vector of Ollama model tags (or backend-appropriate
identifiers).}

\item{replicates}{Integer >= 1. Number of replicates per model.}

\item{aggregator}{One of "mean", "median", "max", "topk_mean".}

\item{temperature}{Sampling temperature passed to the backend.}

\item{backend}{A backend object (default: \code{backend_ollama()}).}
}
\value{
A \code{screenllm_ensemble} object.
}
\description{
Lets the user swap in a different set of Ollama-served models, adjust
the number of replicates per model, or change the aggregation rule.
The paper's findings support 3-4 comparable open-source LLMs with the
\emph{mean} aggregator; other choices are supported but not recommended.
}
