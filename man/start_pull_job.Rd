% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async_pull.R
\name{start_pull_job}
\alias{start_pull_job}
\title{Start a background pull for an Ollama model}
\usage{
start_pull_job(model, ollama_url = getOption("screenllm.ollama_url"))
}
\arguments{
\item{model}{Ollama model tag (e.g. \code{"mistral:7b"}).}

\item{ollama_url}{Base URL of the Ollama server.}
}
\value{
A list with \code{model}, \code{pid}, \code{handle}, and \code{progress_path}.
}
\description{
Spawns a \code{callr::r_bg} subprocess that streams progress from
Ollama's \verb{/api/pull} endpoint into a progress file. The caller can
poll \code{pull_job_status(model)} for updates and \code{pull_job_cancel(handle)}
to abort.
}
\details{
Idempotent per-model within one R session: if a pull is already
running for \code{model}, returns the existing handle instead of
launching another.
}
