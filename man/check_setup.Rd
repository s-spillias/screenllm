% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{check_setup}
\alias{check_setup}
\title{Verify local setup for \code{screenllm}}
\usage{
check_setup(
  models = .PINNED_DEFAULT_MODELS,
  ollama_url = getOption("screenllm.ollama_url")
)
}
\arguments{
\item{models}{Character vector of model tags to require. Defaults to the
four LLMs pinned by \code{default_ensemble()}. Pass \code{NULL} to skip the
model-availability check.}

\item{ollama_url}{Base URL of the local Ollama server. Defaults to the
\code{screenllm.ollama_url} option (usually \verb{http://localhost:11434}).}
}
\value{
Invisible logical.
}
\description{
Confirms that Ollama is reachable at the configured URL, that R can
talk to it, and (optionally) that the four default models are available.
Returns \code{TRUE} invisibly on success and prints a diagnostic panel;
returns \code{FALSE} on failure with actionable messages.
}
\examples{
\dontrun{
check_setup()
check_setup(models = NULL) # just check server, not models
}
}
