% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_anthropic.R
\name{backend_anthropic}
\alias{backend_anthropic}
\title{Anthropic Claude Messages backend}
\usage{
backend_anthropic(
  api_key = Sys.getenv("ANTHROPIC_API_KEY"),
  base_url = "https://api.anthropic.com/v1",
  anthropic_version = "2023-06-01",
  max_tokens = 1024L,
  timeout = 300,
  max_retries = 3L
)
}
\arguments{
\item{api_key}{Anthropic API key. Defaults to
\code{Sys.getenv("ANTHROPIC_API_KEY")}.}

\item{base_url}{Base URL for the Anthropic API.}

\item{anthropic_version}{API version header. \code{"2023-06-01"} is
the long-lived stable version.}

\item{max_tokens}{Maximum completion tokens. 1024 is comfortable
for a per-record score + explanation; raise if you're truncating.}

\item{timeout}{Per-call timeout in seconds.}

\item{max_retries}{Retries on transport or 5xx errors.}
}
\value{
A backend object usable with \code{custom_ensemble()}.
}
\description{
Talks to Anthropic's \verb{/v1/messages} endpoint. Anthropic doesn't
have OpenAI's \code{response_format = json_object} toggle, so we rely
on the prompt schema (screening prompt says "respond ONLY with a
JSON object") and the balanced-braces JSON extraction fallback to
handle occasional prose-wrapping. In practice Claude follows the
schema.
}
\details{
The \code{api_key} is read from \code{ANTHROPIC_API_KEY} by default.

Model tags follow Anthropic's IDs (e.g. \code{"claude-opus-4-5"},
\code{"claude-sonnet-5"}, \code{"claude-haiku-4-5"}).
}
\examples{
\dontrun{
# Set your key first in ~/.Renviron: ANTHROPIC_API_KEY=sk-ant-...
ens <- custom_ensemble(
  models = "claude-sonnet-5",
  replicates = 1L,
  backend = backend_anthropic()
)
}
}
