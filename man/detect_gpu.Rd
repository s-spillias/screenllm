% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpu.R
\name{detect_gpu}
\alias{detect_gpu}
\title{Detect a usable GPU for Ollama inference}
\usage{
detect_gpu()
}
\value{
A list with:
\itemize{
\item \code{available}: logical
\item \code{kind}: one of \code{"apple"}, \code{"nvidia"}, \code{"amd"}, or \code{"none"}
\item \code{detail}: a short human-readable string (e.g. the GPU model
name, or the reason detection failed)
}
}
\description{
Probes the system for a GPU that Ollama can offload models to. Does
not require Ollama itself; the checks are OS-level so users can
verify their hardware is set up before pulling any models.
}
\details{
Checks in order:
\enumerate{
\item macOS with Apple Silicon (Metal is used automatically).
\item \code{nvidia-smi} returning success -- an NVIDIA GPU with a working
CUDA driver.
\item \code{rocm-smi} returning success -- an AMD GPU with a working ROCm
driver.
}

Ollama itself decides how much of a model to offload to the GPU
(based on free VRAM); this function only reports whether the
underlying hardware is available. To verify Ollama is actually
using the GPU during a run, look at \verb{ollama ps} in a terminal: the
\code{SIZE (GPU)} column shows how many bytes are on the GPU.
}
\examples{
info <- detect_gpu()
info$available
info$kind
}
