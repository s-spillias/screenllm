% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{default_ensemble_light}
\alias{default_ensemble_light}
\title{A "light" ensemble that runs on a laptop}
\usage{
default_ensemble_light(
  backend = backend_ollama(),
  replicates = .DEFAULT_REPLICATES,
  temperature = .DEFAULT_TEMPERATURE
)
}
\arguments{
\item{backend}{A backend object (default: \code{backend_ollama()}).}

\item{replicates}{Integer >= 1. Defaults to three.}

\item{temperature}{Sampling temperature. Defaults to 0.7 (per the paper).}
}
\value{
A \code{screenllm_ensemble} object.
}
\description{
Returns a four-LLM mean ensemble built from 3-4 B-class open-weights
models (\verb{gemma3:4b}, \verb{llama3.2:3b}, \verb{qwen3:4b}, \verb{mistral:7b}). Fits in
roughly 10 GB of disk and 8 GB of RAM at Q4 quantisation, so it runs
comfortably on an 8-16 GB laptop.
}
\details{
Slightly less accurate than \code{\link[=default_ensemble]{default_ensemble()}} (~5-10 percentage-point
drop in AP on the paper benchmarks), but useful for exploration, small
reviews, or machines that cannot host the paper's 65 GB ensemble.
}
\examples{
\donttest{
light <- default_ensemble_light(backend = backend_mock())
print(light)
}
}
